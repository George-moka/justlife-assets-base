[build]
  publish = "public"
  functions = "netlify/functions"

# Map /api/generate -> the serverless proxy function
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
